const assert = require('chai').assert;

describe('our test bundle', function () {
  it('should work', function () {
    assert(true);
    });
  });

describe('create a contact', function () {
  context('createNewContact function', function () {

    it.skip('should be a function', function () {
      assert.isFunction(createNewContact);
    });

    it.skip('should create a new contact', function () {

    });

    context('properties of the new contact', function () {

      it.skip('the new contact should have a name', function () {

      });

      it.skip('the new contact should have a company name', function () {

      });

      it.skip('the new contact should have a phone number', function () {

      });

      it.skip('the new contact should have a followup status', function () {

      });

    });

  });
});
